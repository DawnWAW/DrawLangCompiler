// 测试各种注释符的使用情况
// C++风格注释测试
origin is (100, 300);    // 设置原点偏移
rot is PI/6;             // 旋转30度（PI/6弧度）
scale is (100, 100/3);   // 比例设置

-- SQL风格注释测试
origin is (200, 100);    -- 设置新的原点偏移
rot is PI/3;             -- 旋转60度（PI/3弧度）
scale is (50, 50);       -- 新的比例设置

# Python风格注释测试
origin is (50, 150);     # 设置另一个原点偏移
rot is PI/2;             # 旋转90度（PI/2弧度）
scale is (25, 75);       # 另一个比例设置

// 混合使用不同注释符
for T from 0 to 2*PI step PI/50 draw (cos(T), sin(T)); // 绘制圆
-- 绘制另一条线
for T from 0 to 100 step 1 draw (T, 0); -- 水平线
# 绘制垂直线
for T from 0 to 100 step 1 draw (0, T); # 垂直线

// 注释符在行首测试
// 这是一整行注释
-- 这也是一整行注释
# 这还是一整行注释

// 特殊情况测试：注释符相邻使用
origin is (0, 0); // -- 注释符相邻但只有//生效
origin is (10, 10); -- // 注释符相邻但只有--生效
origin is (20, 20); # // 注释符相邻但只有#生效

/* 多个连续的注释行
//
--
#
origin is (30, 30);
origin is (30, 30);
origin is (30, 30);
注释块结束
*/

origin is (30, 30);